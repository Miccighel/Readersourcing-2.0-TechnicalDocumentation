% IMPOSTAZIONI DOCUMENTO 

\documentclass[a4paper, english]{article}

\usepackage[utf8]{inputenc}
\usepackage{babel}
\usepackage[citestyle=numeric,bibstyle=numeric,maxcitenames=1,useprefix,hyperref,natbib,backend=biber]{biblatex}
\usepackage{uniudtesi}
\usepackage[nottoc]{tocbibind}
\usepackage{indentfirst}
\usepackage[counterclockwise]{rotating}
\usepackage{listings}
\usepackage{fancyhdr}
\usepackage{enumerate}
\usepackage{longtable}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{csquotes}
\usepackage{booktabs}
\usepackage{quoting}
\usepackage{amsmath}
\usepackage{adjustbox}
\usepackage{array}
\usepackage{amsfonts,amssymb,amsthm}
\usepackage{latexsym}
\usepackage{caption}
\usepackage{changepage}
\usepackage{enumitem}
\usepackage{threeparttable}
\usepackage{fancyvrb}
\usepackage{makecell}
\usepackage{tocloft}
\usepackage{xstring}
\usepackage{mathtools}
\usepackage{etoolbox}
\usepackage{xcolor,colortbl}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{xspace}
\usepackage{tabularx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,fit,positioning,shapes.multipart}

% NOME DEL FILE CONTENENTE LA BIBLIOGRAFIA. IL FILE DEVE TROVARSI NELLA ROOT DEL FILE .TEX, AVERE IL NOME INDICATO TRA LE PARENTESI GRAFFE ED ESTENSIONE .bib 

\bibliography{bibliography}

% PATH RELATIVO DALLA ROOT DEL FILE TEX ALLA CARTELLA CONTENENTE LE IMMAGINI

\graphicspath{{./figures/}}

\raggedbottom
\setlength{\emergencystretch}{2em}

% IMPOSTAZIONI FONT 

\renewcommand{\baselinestretch}{1.2}

% OPZIONE CHE CONSENTE DI SPEZZARE LE URL IN QUALUNQUE LORO PUNTO

\expandafter\def\expandafter\UrlBreaks\expandafter{\UrlBreaks
\do\a\do\b\do\c\do\d\do\e\do\f\do\g\do\h\do\i\do\j
\do\k\do\l\do\m\do\n\do\o\do\p\do\q\do\r\do\s\do\t
\do\u\do\v\do\w\do\x\do\y\do\z\do\A\do\B\do\C\do\D
\do\E\do\F\do\G\do\H\do\I\do\J\do\K\do\L\do\M\do\N
\do\O\do\P\do\Q\do\R\do\S\do\T\do\U\do\V\do\W\do\X
\do\Y\do\Z}

% COLONNE TABELLA DIMENSIONE FISSATA 

\newcolumntype{L}[1]{>{\raggedright\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{R}[1]{>{\raggedleft\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\tikzset{
  rsbox/.style={
    draw,
    rounded corners=2pt,
    align=left,
    inner sep=5pt,
    fill=white,
    font=\small
  },
  rsclass/.style={
    rectangle split,
    rectangle split parts=2,
    draw,
    align=left,
    inner sep=4pt,
    fill=white,
    font=\small
  },
  rsarrow/.style={-{Latex[length=2mm]}, semithick},
  rsdependency/.style={-{Latex[length=2mm]}, dashed}
}

% NO VIRGOLETTE IN BIBLIOGRAFIA

\DeclareFieldFormat[inbook]{title}{#1} 
\DeclareFieldFormat[inproceedings]{title}{#1} 
\DeclareFieldFormat[article]{title}{#1}

% DEFINIZIONE DI MACRO

\newcommand{\N}{\mathbb{N}}

% LSTLISTINGS CON LINE WRAPPING

\lstset{
basicstyle=\small\ttfamily,
columns=flexible,
breaklines=true
}

% VISUALIZZAZIONE DI FILE JSON

\colorlet{punct}{red!60!black}
\definecolor{background}{HTML}{EEEEEE}
\definecolor{delim}{RGB}{20,105,176}
\colorlet{numb}{magenta!60!black}


\lstdefinelanguage{json}{
    basicstyle=\normalfont\ttfamily,
    numbers=left,
    numberstyle=\scriptsize,
    stepnumber=1,
    numbersep=8pt,
    showstringspaces=false,
    breaklines=true,
    frame=lines,
    backgroundcolor=\color{background},
    literate=
     *{0}{{{\color{numb}0}}}{1}
      {1}{{{\color{numb}1}}}{1}
      {2}{{{\color{numb}2}}}{1}
      {3}{{{\color{numb}3}}}{1}
      {4}{{{\color{numb}4}}}{1}
      {5}{{{\color{numb}5}}}{1}
      {6}{{{\color{numb}6}}}{1}
      {7}{{{\color{numb}7}}}{1}
      {8}{{{\color{numb}8}}}{1}
      {9}{{{\color{numb}9}}}{1}
      {:}{{{\color{punct}{:}}}}{1}
      {,}{{{\color{punct}{,}}}}{1}
      {\{}{{{\color{delim}{\{}}}}{1}
      {\}}{{{\color{delim}{\}}}}}{1}
      {[}{{{\color{delim}{[}}}}{1}
      {]}{{{\color{delim}{]}}}}{1},
}

\makeatletter
\renewcommand{\ALG@name}{Listing}
\renewcommand{\listalgorithmname}{List of Listings}
\makeatother

\titleclass{\subsubsubsection}{straight}[\subsection]

\newcounter{subsubsubsection}[subsubsection]
\renewcommand\thesubsubsubsection{\thesubsubsection.\arabic{subsubsubsection}}
\renewcommand\theparagraph{\thesubsubsubsection.\arabic{paragraph}} % optional; useful if paragraphs are to be numbered

\titleformat{\subsubsubsection}
  {\normalfont\normalsize\bfseries}{\thesubsubsubsection}{1em}{}
\titlespacing*{\subsubsubsection}
{0pt}{3.25ex plus 1ex minus .2ex}{1.5ex plus .2ex}

\makeatletter
\renewcommand\paragraph{\@startsection{paragraph}{5}{\z@}%
  {3.25ex \@plus1ex \@minus.2ex}%
  {-1em}%
  {\normalfont\normalsize\bfseries}}
\renewcommand\subparagraph{\@startsection{subparagraph}{6}{\parindent}%
  {3.25ex \@plus1ex \@minus .2ex}%
  {-1em}%
  {\normalfont\normalsize\bfseries}}
\def\toclevel@subsubsubsection{4}
\def\toclevel@paragraph{5}
\def\toclevel@paragraph{6}
\def\l@subsubsubsection{\@dottedtocline{4}{7em}{4em}}
\def\l@paragraph{\@dottedtocline{5}{10em}{5em}}
\def\l@subparagraph{\@dottedtocline{6}{14em}{6em}}
\makeatother

\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{4}

\newcommand{\rsserver}{RS\_Server\xspace}
\newcommand{\rspdf}{RS\_PDF\xspace}
\newcommand{\rsrate}{RS\_Rate\xspace}
\newcommand{\rspy}{RS\_Py\xspace}

\lstset{
columns=flexible,
breaklines=true
}

% INFORMAZIONI GENERALI

\titolo{Readersourcing 2.0:\\ Documentation}
\nomeprogetto{MICHAEL SOPRANO AND STEFANO MIZZARO}

\begin{document}

\pagenumbering{roman}
\hypersetup{pageanchor=false}
\maketitle
\hypersetup{pageanchor=true}
\setcounter{page}{1}

\tableofcontents

\newpage

\listoffigures

\newpage

\listoftables

\newpage
\pagenumbering{arabic}


\section{Introduction}


The Readersourcing 2.0 ecosystem was developed as part of a research project co-funded by SISSA Medialab\footnote{\url{https://medialab.sissa.it/}} and the University of Udine.\footnote{\url{https://www.uniud.it/}} It was presented by \citet{Soprano2019} at the IRCDL 2019 Conference.\footnote{\url{https://ircdl2019.isti.cnr.it/}} The paper is also available on Zenodo.\footnote{\url{https://zenodo.org/record/1446468}} The code and related documentation are available on GitHub.\footnote{\url{https://github.com/Miccighel/Readersourcing-2.0}}

Initially, a recap of its general architecture is presented, followed by a brief description of the role and purpose of each of its components. Specific aspects, such as the technology used, the internal architecture, and the structure of the database, are also discussed. This is achieved through different types of diagrams adhering to the UML standard unless otherwise specified, drawn according to the style rules proposed by \citet{Fowler03}. The current software requirements and operational procedures are recorded alongside the original architectural description.

\section{General Architecture}

Readersourcing 2.0 is an ecosystem composed of several components. It includes \rsserver \cite{RS-Server} (presented in Section~\ref{rs-server}), which gathers the ratings given by readers, and \rsrate \cite{RS-Rate} (presented in Section~\ref{rs-rate}), which acts as a client and enables readers to rate publications. Although all operations can be carried out directly through the web interface provided by \rsserver, \rspdf \cite{RS-PDF} (presented in Section~\ref{rs-pdf}) annotates PDF files with a rating URL and QR code on behalf of the server-side application. Finally, \rspy (presented in Section~\ref{rs-py}) provides a fully functional Python implementation of the RSM and TRM models described by \citet{Soprano2019}, together with the notebooks used to generate datasets and analyze experiments.

Figure~\ref{fig:architecture} provides an overview of the architecture of Readersourcing 2.0, and in the following, we briefly describe these four components. We summarize the capabilities of the ecosystem in Section~\ref{overview}.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/architecture.png}
\caption{Conceptual architecture of Readersourcing 2.0 (NOT UML). \rspdf is bundled with and invoked by \rsserver.}
\label{fig:architecture}
\end{figure}

\section{\rsserver}

\label{rs-server}

\rsserver \cite{RS-Server} is the server-side component responsible for collecting and aggregating the ratings given by readers and applying the RSM and TRM models described by \citet{Soprano2019} to compute quality scores for readers and publications. An instance of \rsserver is deployed with a compatible \rspdf executable, which is shipped in the \verb|lib| directory and invoked as a local process when a publication must be annotated.
Up to \emph{n} browsers and their users can communicate with the server, each through an instance of \rsrate. The extension is distributed for Chromium-based browsers and Firefox.

Readers can interact with the server either through clients installed in their browsers or through the stand-alone web interface provided by \rsserver. These clients manage reader registration and authentication, rating actions, and downloads of link-annotated publications.

During the design phase of \rsserver, strategies were adopted to ensure extensibility and generality. This includes: 
\begin{enumerate}[label=(\roman*)]
\item Straightforward addition of new models. 
\item A shared input data format among all models.
\item A standard procedure for models needing to save values locally in the \rsserver database.
\end{enumerate}

\subsection{Implementation and Technology}

\rsserver is developed using \emph{Ruby on Rails},\footnote{\url{https://rubyonrails.org/}} an open-source web application framework built on the Ruby programming language and strongly based on the Model-View-Controller (MVC) architectural pattern.

The MVC pattern separates a program's control logic from its data presentation and business logic, helping developers establish a clear architecture from the initial design phase. Figure~\ref{fig:mvc} illustrates the resulting structure. It comprises three distinct entities: the \emph{Controller}, which manages control logic; the \emph{Model}, which encapsulates business logic; and the \emph{View}, which presents data.

\begin{figure}[!tbp]
\centering
\includegraphics[scale=0.7]{figures/mvc.png}
\caption{Intuitive scheme of the MVC pattern (NOT UML).}
\label{fig:mvc}
\end{figure}

The Controller has direct access to both the Model and the View. It typically receives user input from the View, updates the internal state of the Model through its methods, and then passes the updated Model back to the View for presentation. A software system may contain several Controllers, each of which can manage multiple Model instances. In web MVC frameworks such as Rails, Controllers are commonly organized around domain resources, and a Model can be presented through more than one View.

MVC is not the only principle underlying Rails. Another central principle is \lq\lq Convention Over Configuration\rq\rq{}: the framework reduces the number of decisions required during application development by providing standard conventions that can still be changed when greater flexibility is needed. The \lq\lq Rails Doctrine\rq\rq{} describes these principles in greater detail.\footnote{\url{https://rubyonrails.org/doctrine/}}

Rails is an actively developed framework used by organizations such as \emph{GitHub} and \emph{SoundCloud}. It is supported by an established community and extensive learning resources.

\subsection{Communication Paradigm}

An MVC framework such as Rails can support several kinds of web applications, including a \emph{Web Service}. A Web Service exposes operations remotely by exchanging messages encoded in a standard format such as \emph{JSON} over a transport protocol such as \emph{HTTP}. Its interface must define the available resources and operations, together with the messages required to access them.

One communication paradigm for Web Services is \emph{REST} (\emph{REpresentational State Transfer}). In a RESTful interface, resources are identified by URIs and the HTTP method determines the operation to perform. The server returns a response in the agreed interchange format, and the client interprets that response.

\rsserver is a Rails application exposing RESTful interfaces alongside server-rendered web workflows. API clients exchange JSON messages over HTTP, while the same domain and authorization rules are reused by the web interface.

The REST interface is defined by the application routes, JSON views, and integration tests. A Postman collection provides request and response examples for the exposed resources:
\begin{center}
\small\url{https://documenter.getpostman.com/view/4632696/RWTiwfV4?version=latest}.
\end{center}

For example, Table~\ref{tab:rest} presents a subset of the RESTful interface of \rsserver. These operations manage publications, one of the entities in the application domain. If a user requests the ``show'' operation for publication 1 through the corresponding endpoint, \rsserver returns a JSON response similar to the following example.

\begin{lstlisting}[language=json,firstnumber=1]
{
  "id": 1,
  "doi": "10.1140/epjc/s10052-018-6047-y",
  "title": "Uncertainties in WIMP dark matter scattering revisited",
  "subject": null,
  "author": "John Ellis",
  "creator": "Springer",
  "producer": null,
  "pdf_url": "https://example.org/publication.pdf",
  "steadiness": 1.0,
  "score_rsm": 0.71,
  "score_trm": 0.68,
  "created_at": "2026-01-01T12:00:00.000Z",
  "updated_at": "2026-01-01T12:00:00.000Z",
  "url": "http://localhost:3000/publications/1.json"
}
\end{lstlisting}

\begin{table}
\centering
\begin{threeparttable}
\scriptsize
\begin{tabularx}{\textwidth}{p{4.9cm}C{1.2cm}C{1.5cm}Y}
\toprule
\textbf{Endpoint}  &\textbf{HTTP Message} & \textbf{Operation} & \textbf{Description} \\
\midrule
\path{/publications.json} & GET & Index & Fetches the entire collection of Publications. \\
\midrule
\path{/publications/list} & GET & List & Fetches the entire collection of Publications (server-side view). \\
\midrule
\path{/publications/1.json} & GET & Show & Returns the Publication with identifier equal to 1.\\
\midrule
\path{/publications/lookup.json} & POST & Lookup & Searches for a Publication; if it does not exist, it is fetched from the given URL. \\
\midrule
\path{/publications/random.json} & GET & Random & Returns a random Publication. \\
\midrule
\path{/publications/1/is_rated.json} & GET & Is Rated & Checks if the Publication with identifier equal to 1 has been rated by at least one reader. \\
\midrule
\path{/publications/1/is_saved_for_later.json} & GET & Is Saved For Later & Checks if the Publication with identifier equal to 1 has been saved for later by the current user. \\
\midrule
\path{/publications.json} & POST & Create & Creates a new Publication. \\
\midrule
\path{/publications/is_fetchable.json} & POST & Is Fetchable & Checks if the provided URL contains a fetchable Publication. \\
\midrule
\path{/publications/extract.json} & POST & Extract & Extracts the rating URL from a link-annotated Publication. \\
\midrule
\path{/publications/fetch.json} & POST & Fetch & Fetches a Publication from the given URL. \\
\midrule
\path{/publications/1/refresh.json} & GET & Refresh & Fetches the Publication with identifier equal to 1 again. \\
\midrule
\path{/publications/1.json} & PUT & Update & Updates the Publication with identifier equal to 1. \\
\midrule
\path{/publications/1.json} & DELETE & Delete & Deletes the Publication with identifier equal to 1.\\
\midrule
\dots & \dots & \dots & \dots \\
\bottomrule
\end{tabularx}
\caption{Subset of the RESTful interface of \rsserver.}
\label{tab:rest}
 \end{threeparttable}
\end{table}

\subsection{Database}

To support features such as storing link-annotated publications and authenticating users, \rsserver uses the database schema illustrated in Figure~\ref{fig:er}. The Readersourcing 2.0 application domain contains three main entities:

\begin{itemize}
\item \textbf{Users}: represents the users of the system and stores their personal data. A user may have an optional \emph{ORCID} and a boolean indicating whether they wish to receive emails. Additional attributes store the tokens required for operations such as password reset.
\item \textbf{Ratings}: represents the ratings provided by readers for publications. Each rating has a numerical score, a boolean indicating whether it is anonymous, and another boolean recording whether it was edited later. A reader can provide at most one rating for a given publication; this invariant is enforced both by the application model and by a unique database constraint.
\item \textbf{Publications}: represents publications known to the server, including those saved for later but not yet rated. A publication may have a \emph{DOI}, descriptive metadata, and attributes used to manage the original and link-annotated PDF files.
\end{itemize}
These entities also contain attributes such as \emph{steadiness} and \emph{informativeness}, which store scores or parameters computed by the Readersourcing models.

An auxiliary \textbf{AuthenticationToken} entity records each active login. It belongs to one user and stores the unique JWT identifier and its expiration time, but never the encoded JWT itself. Authentication tokens support the security boundary rather than the Readersourcing score domain.

\begin{figure}[!tbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[node distance=2.1cm]
  \node[rsclass] (user) {
    \textbf{User}
    \nodepart{second}
    \begin{tabular}{@{}l@{}}
      id (PK)\\
      email, first\_name, last\_name\\
      orcid, password\_digest\\
      email\_confirmed, subscribe\\
      score, steadiness, bonus
    \end{tabular}
  };
  \node[rsclass, right=of user] (rating) {
    \textbf{Rating}
    \nodepart{second}
    \begin{tabular}{@{}l@{}}
      id (PK)\\
      user\_id (FK)\\
      publication\_id (FK)\\
      score, original\_score\\
      anonymous, edited\\
      goodness, informativeness\\
      accuracy\_loss, bonus
    \end{tabular}
  };
  \node[rsclass, right=of rating] (publication) {
    \textbf{Publication}
    \nodepart{second}
    \begin{tabular}{@{}l@{}}
      id (PK)\\
      doi, title, subject, author\\
      creator, producer, pdf\_url\\
      PDF storage attributes\\
      score\_rsm, score\_trm\\
      steadiness
    \end{tabular}
  };
  \node[rsclass, below=of user] (authenticationtoken) {
    \textbf{AuthenticationToken}
    \nodepart{second}
    \begin{tabular}{@{}l@{}}
      id (PK)\\
      user\_id (FK)\\
      jti\\
      expires\_at
    \end{tabular}
  };

  \draw[rsarrow] (rating.west) -- node[above,pos=.13] {\scriptsize $0..*$}
    node[above,pos=.88] {\scriptsize $1$} node[below] {\scriptsize belongs to} (user.east);
  \draw[rsarrow] (rating.east) -- node[above,pos=.13] {\scriptsize $0..*$}
    node[above,pos=.88] {\scriptsize $1$} node[below] {\scriptsize belongs to} (publication.west);
  \draw[rsarrow] (authenticationtoken.north) -- node[right,pos=.13] {\scriptsize $0..*$}
    node[right,pos=.88] {\scriptsize $1$} node[left] {\scriptsize belongs to} (user.south);
\end{tikzpicture}%
}
\caption{Persistent entities and associations in the \rsserver database (selected attributes).}
\label{fig:er}
\end{figure}

Figure~\ref{fig:er} depicts the two mandatory associations of a rating. A user may give zero or more ratings, but each rating belongs to exactly one user. An anonymous rating still retains this association so that authentication and the one-rating-per-user constraint can be enforced; anonymity controls how the reader identity participates in presentation and processing, not referential integrity.

Similarly, a publication may receive zero or more ratings, while each rating belongs to exactly one publication. The zero multiplicity is required because a publication may be fetched and saved for later before any rating is submitted.

The auxiliary association permits a user to have zero or more active authentication tokens, while every token belongs to exactly one user. Removing a user also removes all their active tokens.

\subsection{Class Diagram}

Figure~\ref{fig:class-1} shows the main responsibilities of the \rsserver implementation. Rails controllers map HTTP operations to the domain models, while JSON and server-rendered views present their results. The three persistent domain models encapsulate the Readersourcing logic and associations described in the previous section. The auxiliary \emph{AuthenticationToken} model maintains the active-login registry, while non-persistent objects represent durable paper references and request-rate policies. Authentication, reference validation, request throttling, and Readersourcing computations are delegated to focused objects rather than being absorbed into the controllers.

\begin{figure}[!tbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[node distance=1.3cm and 1.5cm]
  \node[rsclass] (application) {
    \textbf{ApplicationController}
    \nodepart{second}
    web and shared actions\\
    authorize\_server\_request()\\
    authorize\_api\_request()
  };
  \node[rsclass, below left=of application] (domaincontrollers) {
    \textbf{Domain Controllers}
    \nodepart{second}
    PublicationsController\\
    UsersController\\
    RatingsController\\
    PasswordsController
  };
  \node[rsclass, below right=of application] (authenticationcontroller) {
    \textbf{AuthenticationController}
    \nodepart{second}
    login(), logout()\\
    authenticate()
  };

  \node[rsclass, below=of domaincontrollers, xshift=-3.2cm] (user) {
    \textbf{User}
    \nodepart{second}
    authentication and profile\\
    reader scores and ratings
  };
  \node[rsclass, right=of user] (rating) {
    \textbf{Rating}
    \nodepart{second}
    normalize\_score()\\
    compute\_scores()
  };
  \node[rsclass, right=of rating] (publication) {
    \textbf{Publication}
    \nodepart{second}
    PDF lifecycle and metadata\\
    rating history and scores
  };

  \node[rsclass, below=of authenticationcontroller] (authenticator) {
    \textbf{Authenticator}
    \nodepart{second}
    validate credentials\\
    issue token
  };
  \node[rsclass, right=of authenticator] (authorizer) {
    \textbf{Authorizer}
    \nodepart{second}
    decode and validate token\\
    resolve current user
  };
  \node[rsclass, below=of authorizer] (jwt) {
    \textbf{JsonWebToken}
    \nodepart{second}
    encode(), decode()
  };
  \node[rsclass, below=of jwt] (paperreference) {
    \textbf{PaperRatingReference}
    \nodepart{second}
    issue()\\
    resolve()
  };

  \node[rsclass, below=2.0cm of rating] (readersourcing) {
    \textbf{Readersourcing}
    \nodepart{second}
    strategy\\
    compute\_scores()
  };
  \node[rsclass, below=of readersourcing] (strategy) {
    \textit{ReadersourcingStrategy}
    \nodepart{second}
    compute\_scores()
  };
  \node[rsclass, below left=of strategy] (rsm) {
    \textbf{RsmStrategy}
    \nodepart{second}
    incremental RSM update
  };
  \node[rsclass, below right=of strategy] (trm) {
    \textbf{TrmStrategy}
    \nodepart{second}
    historical TRM update
  };

  \draw[rsarrow] (domaincontrollers) -- (application);
  \draw[rsarrow] (authenticationcontroller) -- (application);
  \draw[rsarrow] (domaincontrollers) -- (user);
  \draw[rsarrow] (domaincontrollers) -- (rating);
  \draw[rsarrow] (domaincontrollers) -- (publication);
  \draw[rsarrow] (authenticationcontroller) -- (authenticator);
  \draw[rsdependency] (application) -- (authorizer);
  \draw[rsdependency] (authenticator) -- (jwt);
  \draw[rsdependency] (authorizer) -- (jwt);
  \draw[rsdependency] (publication) -- (paperreference);
  \draw[rsarrow] (rating) -- (readersourcing);
  \draw[rsarrow] (readersourcing) -- node[right] {\scriptsize delegates} (strategy);
  \draw[rsarrow] (rsm) -- (strategy);
  \draw[rsarrow] (trm) -- (strategy);
  \draw[rsarrow] (rating) -- (user);
  \draw[rsarrow] (rating) -- (publication);
\end{tikzpicture}%
}
\caption{High-level class diagram of \rsserver. Method lists are intentionally selective.}
\label{fig:class-1}
\end{figure}

The \emph{ApplicationController} contains shared authorization hooks for both API and server-side requests, while the \emph{AuthenticationController} handles login and logout. The \emph{Authenticator}, \emph{Authorizer}, and \emph{AuthenticationTokenRevoker} command objects isolate credential verification, token issuance, token validation, and revocation from HTTP concerns.

The \emph{PaperRatingReference} value object similarly isolates the creation and validation of references stored in link-annotated publications. It uses authenticated encryption with a dedicated purpose and binds each reference to one reader and one publication without persisting a login credential in the PDF.

The \emph{RequestRateLimit} value object describes each request budget and provides opaque identities for the corresponding cache counters. Controllers apply these policies through the Rails rate-limiting boundary, leaving credential verification, mail delivery, publication handling, and the Readersourcing domain objects unaware of throttling concerns.

For REST clients, relying only on server-side session data to identify the logged-in user is unsuitable because the RESTful interface is \emph{stateless}. Clients therefore attach an authentication \emph{token} to each protected request. The server-rendered workflow can still keep the same token in its Rails session.

Authentication establishes the identity of the reader making the request, while resource-level authorization determines which records that reader may operate on. Profile updates, account deletion, and subscription changes are restricted to the current reader. A reader may inspect and edit only their own rating through the individual rating endpoints. Reader collections and individual reader responses expose the public academic profile---name, ORCID, and Readersourcing scores---without email or subscription preferences; the current reader obtains those private profile fields through the dedicated information endpoint.

When a user logs in, the supplied credentials are verified against the stored password digest and the account confirmation state. The credentials themselves are not placed in the token. If verification succeeds, \rsserver creates an active-token record and issues an HS256-signed JWT whose payload contains the user identifier, the client IP address, its unique \verb|jti| identifier, and a standard expiration claim set to seven days after issuance.

The browser client stores the returned token in its application cookie and attaches it to subsequent API requests through the \verb|Authorization| header. The header accepts both the direct token format used by \rsrate and the conventional \verb|Bearer <token>| format. The server-rendered workflow also keeps a duplicate token in the Rails session, which is implemented through an HTTP-only cookie. Upon receiving a protected request, the \emph{Authorizer} verifies the JWT signature, expiration time, IP address, referenced user, and corresponding active-token record before allowing the request to proceed. Figure~\ref{fig:auth} provides an intuitive illustration of this authentication process.

The Rails session identifier is renewed after successful authentication, before the token is stored, and is renewed again when the reader logs out. Logout removes the active-token record for the current browser or API session and leaves the reader's other devices authenticated. A successful password change or reset removes every active token belonging to that reader; account deletion removes the same records through the user association. The signed value remains immutable, but without its active record it can no longer authorize a protected request.

Logout is a state-changing operation and is therefore available only through an HTTP \verb|POST| request. HTML state-changing requests require a valid Rails authenticity token. JSON API requests remain stateless: a request carrying a JSON body, or an authenticated JSON request carrying an \verb|Authorization| header, is handled through the token-based API boundary instead of the browser-session CSRF boundary.

In the save-for-later workflow, \rsserver places a dedicated paper-rating reference in the URL supplied to \rspdf. Its encrypted payload contains the reader and publication identifiers and has no automatic expiration. It is separate from the seven-day JWT and cannot authorize an API request. Following the reference always requires a current authenticated session, after which the server verifies that the reference belongs to both the selected publication and the authenticated reader. Logout, password changes, and password resets therefore do not invalidate an annotated PDF. Removing its reader or publication makes the reference unusable.

Password recovery uses a separate, single-use credential. An HTTP \verb|GET| request only displays the recovery form; sending the request requires \verb|POST|. The request endpoint returns the same confirmation for registered and unregistered addresses. For a registered address, \rsserver generates a random token, stores only its SHA-256 digest and issue time, and sends a link built from the configured public origin. The link remains valid for four hours. It opens a server-rendered form in which the reader chooses and confirms a new password; a successful reset clears the stored digest before a confirmation email is sent. Passwords are never sent by email.

Request-rate policies bound operations that can be abused or consume disproportionate resources. By default, one IP address may make ten authentication attempts in three minutes and five password-recovery requests in fifteen minutes; one normalized email address may receive three recovery requests in thirty minutes; one IP address may send five contact messages in ten minutes; and one authenticated reader may initiate thirty combined publication-download, inspection, extraction, and annotation operations in one hour. Exceeding a budget returns HTTP status \verb|429 Too Many Requests| with a \verb|Retry-After| header and does not execute the protected action. Cache identities are SHA-256 digests rather than plain email addresses or IP values. Displaying the password-recovery form does not consume a recovery request.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/autenticazione.png}
\caption{Representation of the token-based authentication process (NOT UML).}
\label{fig:auth}
\end{figure}

The classes responsible for the Readersourcing computations follow the \emph{Strategy} pattern: \emph{Readersourcing}, \emph{ReadersourcingStrategy}, \emph{RsmStrategy}, and \emph{TrmStrategy}. A rating invokes the RSM strategy first and the TRM strategy second. The pattern keeps model selection extensible while expressing the score computation order explicitly.

\subsection{Deploy}

\rsserver supports two setup paths: a manual installation, intended primarily for development, and a Docker Compose deployment, which provides a reproducible application and database environment.

Environment-specific secrets and database settings are described in Section~\ref{env-vars}. A local \verb|.env| file must never be committed.

\subsubsection{Modality 1: Manual}

\label{deploy1}

This modality downloads and initializes \rsserver directly on the local machine and offers the greatest flexibility for development and domain-model experiments.

\subsubsubsection{Requirements}

\begin{itemize}
\item Ruby $==$ 3.4.10;
\item Bundler compatible with the committed \verb|Gemfile.lock|;
\item a Java 21 runtime, required by \rspdf;
\item PostgreSQL $>=$ 17;
\item Node.js $>=$ 18, used to install and precompile the existing browser assets.
\end{itemize}

\subsubsubsection{How To}

Clone the \rsserver repository,\footnote{\url{https://github.com/Miccighel/Readersourcing-2.0-RS_Server}} navigate to its root directory, create the \verb|.env| file described in Section~\ref{set-var}, and run:

\begin{lstlisting}
bundle install
node .yarn/releases/yarn-3.6.3.cjs install --immutable
bin/rails db:create
bin/rails db:migrate
bin/rails db:seed    # optional sample data
bin/rails server -b 127.0.0.1 -p 3000 -e development
\end{lstlisting}

PostgreSQL must be accepting connections before the database commands are executed. With the sample bind address and port, the application is available at \url{http://127.0.0.1:3000}. Rails binstubs are always invoked from the repository root so that the locked application dependencies are used.

The active-token registry is populated at login. A session carrying a JWT without a registered identifier is rejected and the reader signs in again.

\subsubsubsection{Quick Cheatsheet}

\begin{enumerate}
\item change to the main directory with \verb|cd|;
\item create and populate the \verb|.env| file;
\item \verb|bundle install|;
\item \verb|node .yarn/releases/yarn-3.6.3.cjs install --immutable|;
\item \verb|bin/rails db:create|;
\item \verb|bin/rails db:migrate|;
\item \verb|bin/rails db:seed| (optional);
\item \verb|bin/rails server -b <address> -p <port> -e development|.
\end{enumerate}

\subsubsection{Modality 2: Docker Compose}

Docker Compose builds \rsserver from the project source tree and starts it together with PostgreSQL 17. The application image includes Ruby 3.4.10, the Java 21 runtime required by \rspdf, the locked Ruby dependencies, and the precompiled browser assets. This is the recommended reproducible deployment for local integration and production-like verification.

\subsubsubsection{Requirements}

\begin{itemize}
\item Docker Desktop or another installation supporting Docker Compose v2.\footnote{\url{https://docs.docker.com/compose/}}
\end{itemize}

\subsubsubsection{How To}

Clone the repository, create the \verb|.env| file, ensure that the Docker Engine is running, and execute:

\begin{lstlisting}
docker compose up --build
\end{lstlisting}

The \verb|database| service must pass its health check before the \verb|rs_server_webapp| service starts. The application entrypoint runs \verb|bin/rails db:create| and \verb|bin/rails db:migrate| automatically. Once startup completes, the web application is available at \url{http://localhost:3000}; \verb|0.0.0.0| is the container bind address, not the address users should enter in a browser.

Sample data remain opt-in:

\begin{lstlisting}
docker compose run --rm rs_server_webapp bin/rails db:seed
\end{lstlisting}

Use \verb|docker compose down| to stop the services. The named PostgreSQL volume is retained so that application data survive ordinary container recreation.

\paragraph{Quick Cheatsheet}

\begin{itemize}
\item change to the main directory with \verb|cd|;
\item create and populate the \verb|.env| file;
\item \verb|docker compose up --build|;
\item \verb|docker compose run --rm rs_server_webapp bin/rails db:seed| (optional);
\item \verb|docker compose down| (to stop the services).
\end{itemize}

\subsubsection{Environment Variables}

\label{env-vars}

Deployment-specific credentials and connection details are supplied through environment variables. Table~\ref{env} lists the variables used by the application. Database URLs take precedence over individual PostgreSQL settings.

\small
\begin{longtable}{L{4.1cm}L{5.7cm}L{2.9cm}}
\toprule
\textbf{Environment Variable} & \textbf{Purpose} & \textbf{Scope} \\
\midrule
\endhead
\verb|SECRET_DEV_KEY| & Rails secret used in development. & development \\
\midrule
\verb|SECRET_PROD_KEY| & Rails secret used to sign and encrypt production data, including JWTs, session data, and paper-rating references. Keep it stable while issued references must remain usable. & production \\
\midrule
\verb|DATABASE_URL| & Complete PostgreSQL connection URL. Overrides the individual \verb|POSTGRES_*| variables. & development, production \\
\midrule
\verb|TEST_DATABASE_URL| & Complete PostgreSQL connection URL for the test database. & test \\
\midrule
\verb|POSTGRES_USER| & PostgreSQL user name used when a full URL is not supplied. & all database environments \\
\midrule
\verb|POSTGRES_PASSWORD| & Password for the PostgreSQL user. & all database environments \\
\midrule
\verb|POSTGRES_HOST| & PostgreSQL host; defaults to \verb|localhost| outside Compose and is set to \verb|database| by Compose. & all database environments \\
\midrule
\verb|POSTGRES_DB| & Development or production database name; defaults to \verb|rs_server|. & development, production \\
\midrule
\verb|POSTGRES_TEST_DB| & Test database name; defaults to \verb|rs_server_test|. & test \\
\midrule
\verb|SMTP_USERNAME|, \verb|SMTP_PASSWORD| & Credentials for the configured SMTP service. & production mail \\
\midrule
\verb|SMTP_DOMAIN_NAME|, \verb|SMTP_DOMAIN_ADDRESS| & Sender domain and SMTP server address. & production mail \\
\midrule
\verb|EMAIL_BUG_REPORT|, \verb|EMAIL_ADMIN| & Recipient addresses for reports and general contact messages. & development, production \\
\midrule
\verb|RAILS_LOG_TO_STDOUT| & When present, sends production Rails logs to standard output. & production \\
\midrule
\verb|RAILS_MAX_THREADS| & Maximum Active Record connection pool size; defaults to 5. & all environments \\
\midrule
\verb|PUBLIC_BASE_URL| & Public HTTP or HTTPS origin used to generate password-recovery links; required for recovery in production. & production \\
\midrule
\path{CORS_ALLOWED_ORIGINS} & Comma-separated origins allowed to make cross-origin API requests. An omitted value disables CORS in production. & production \\
\midrule
\verb|FORCE_SSL| & Set to \verb|true| when the public instance is served through HTTPS. & production \\
\midrule
\makecell[l]{\texttt{RS\_PDF\_MAX\_}\\\texttt{DOWNLOAD\_BYTES}} & Maximum accepted publication size in bytes; defaults to 52428800 (50 MiB). & development, production \\
\midrule
\verb|RS_PDF_OPEN_TIMEOUT| & Maximum connection-opening time in seconds; defaults to 5. & development, production \\
\midrule
\verb|RS_PDF_READ_TIMEOUT| & Maximum response-reading time in seconds; defaults to 20. & development, production \\
\midrule
\path{RS_PDF_PROCESS_TIMEOUT} & Maximum \rspdf execution time in seconds; defaults to 60. & development, production \\
\midrule
\makecell[l]{\texttt{RS\_PDF\_ALLOW\_}\\\texttt{PRIVATE\_NETWORKS}} & Set to \verb|true| only when publications must be fetched from a trusted private network. & development, production \\
\midrule
\makecell[l]{\texttt{RS\_AUTHENTICATION\_}\\\texttt{RATE\_LIMIT}} & Authentication attempts allowed for one IP address in three minutes; defaults to 10. & development, production \\
\midrule
\makecell[l]{\texttt{RS\_PASSWORD\_RECOVERY\_IP\_}\\\texttt{RATE\_LIMIT}} & Recovery requests allowed for one IP address in fifteen minutes; defaults to 5. & development, production \\
\midrule
\makecell[l]{\texttt{RS\_PASSWORD\_RECOVERY\_}\\\texttt{ACCOUNT\_RATE\_LIMIT}} & Recovery requests allowed for one normalized email address in thirty minutes; defaults to 3. & development, production \\
\midrule
\verb|RS_CONTACT_RATE_LIMIT| & Contact messages allowed for one IP address in ten minutes; defaults to 5. & development, production \\
\midrule
\makecell[l]{\texttt{RS\_PDF\_PROCESSING\_}\\\texttt{RATE\_LIMIT}} & Combined publication-processing requests allowed for one reader in one hour; defaults to 30. & development, production \\
\bottomrule
\caption{Environment variables of \rsserver.}
\label{env}
\end{longtable}
\normalsize

\subsubsection{Setting Variables}

\label{set-var}

For local and Compose deployments, create a \verb|.env| file in the \rsserver root and populate it using \verb|key=value| entries. Do not quote secrets unless the selected environment loader requires it, and never commit the file.

The following listing shows a valid \verb|.env| file:

\begin{lstlisting}
SECRET_PROD_KEY=replace-with-a-long-random-secret
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace-with-a-database-password
POSTGRES_DB=rs_server
SMTP_USERNAME=replace-with-an-smtp-user
SMTP_PASSWORD=replace-with-an-smtp-password
SMTP_DOMAIN_NAME=readersourcing.example
SMTP_DOMAIN_ADDRESS=smtp.example
EMAIL_BUG_REPORT=bugs@readersourcing.example
EMAIL_ADMIN=contact@readersourcing.example
PUBLIC_BASE_URL=https://readersourcing.example
CORS_ALLOWED_ORIGINS=https://client.readersourcing.example
FORCE_SSL=true
RAILS_LOG_TO_STDOUT=true
\end{lstlisting}

The value of \verb|PUBLIC_BASE_URL| is an origin: it contains the scheme, host, and optional port, but no path, query string, fragment, or credentials. In production, a missing value makes password recovery unavailable rather than deriving a link from an untrusted request host.

\verb|CORS_ALLOWED_ORIGINS| contains exact origins separated by commas. Supplying \verb|*| deliberately opens the cross-origin policy and is inappropriate for an ordinary public deployment. \rsrate requests browser permission for the selected server origin and therefore does not depend on a generated extension origin being listed here. Set \verb|FORCE_SSL=true| only after HTTPS is available at the public origin.

Publication downloads reject private and reserved network addresses by default. The \verb|RS_PDF_ALLOW_PRIVATE_NETWORKS| override is intended only for a controlled deployment in which trusted publications are deliberately served from such a network.

Rate-limit counters use the configured Rails cache store. The supplied Puma configuration runs a single process and uses an in-memory store. Deployments with multiple server processes or replicas must configure a shared Active Support cache store so that every instance contributes to the same request budgets.

\subsubsection{Sending Mails}

\rsserver supports standard SMTP services for account confirmation, password recovery, password-change notifications, bug reports, and contact messages. Recovery messages contain the single-use link described above; the new password is entered only in \rsserver and is not included in either recovery or confirmation mail. The SMTP provider must expose a host, port 587 with STARTTLS, a user name, a password, and a verified sender domain. Provider-specific API keys can be placed in \verb|SMTP_PASSWORD| when the provider uses an API key as its SMTP credential; the provider documentation defines the required values.

\subsubsection{Connecting To The Database}

A full connection string supplied through \verb|DATABASE_URL| takes precedence over the individual \verb|POSTGRES_*| variables in development and production. The test environment follows the same rule using \verb|TEST_DATABASE_URL| and \verb|POSTGRES_TEST_DB|.
\begin{lstlisting}[basicstyle=\scriptsize\ttfamily]
ENV['DATABASE_URL'] ||
  "postgresql://#{ENV['POSTGRES_USER'] || 'postgres'}:" \
  "#{ENV['POSTGRES_PASSWORD']}@" \
  "#{ENV['POSTGRES_HOST'] || 'localhost'}/" \
  "#{ENV['POSTGRES_DB'] || 'rs_server'}"
\end{lstlisting}

\subsubsection{Logging To The Standard Output}

Development writes logs to standard output by default. In production, setting \verb|RAILS_LOG_TO_STDOUT| configures a tagged logger on standard output, which is appropriate for Docker and other distributed deployments.

\section{\rspdf}

\label{rs-pdf}

\rspdf \cite{RS-PDF} is the software library used by \rsserver to edit PDF files and add the required URL when a reader requests to save a publication for later reading. The current implementation appends a final page containing a clickable rating URL and a QR code, and stores the same URL in the PDF \verb|BaseUrl| metadata field. In this integration, \rspdf treats the complete URL as opaque data: the paper-rating reference is created and later validated only by \rsserver.

Its command-line interface allows \rsserver to invoke it directly. Because the components are deployed together, they do not require a separate network service or communication protocol. The executable is distributed as the self-contained \verb|RS_PDF-v2.0.0-shaded.jar| and bundled in the \rsserver \verb|lib| directory.

\subsection{Implementation and Technology}

\rspdf is developed in Kotlin, an object-oriented programming language that interoperates with the Java Virtual Machine. This interoperability allows developers to reuse software distributed in JAR format and call Java classes directly from Kotlin. The current build targets Java 21 and uses Kotlin 2.4.

This programming language was chosen because it incorporates many modern features and receives robust support. More importantly, the underlying tool used to edit PDF files is \emph{PDFBox},\footnote{\url{https://pdfbox.apache.org/}} a Java library that provides a complete PDF manipulation toolkit. Therefore, \rspdf serves as a wrapper for PDFBox and adds the required reference to the PDFs requested by readers. ZXing\footnote{\url{https://github.com/zxing/zxing}} generates the QR code placed beside the link annotation, Apache Commons CLI parses the execution parameters, and Log4j 2 provides logging.


Kotlin was created by JetBrains,\footnote{\url{https://www.jetbrains.com/}} which announced first-class support for Android development with Google in 2017.\footnote{\url{https://blog.jetbrains.com/kotlin/2017/05/kotlin-on-android-now-official/}} In the same year, JetBrains also announced Kotlin/Native, which compiles Kotlin programs to native binaries without requiring the JVM.

The Kotlin documentation includes an official comparison with Java,\footnote{\url{https://kotlinlang.org/docs/reference/comparison-to-java.html}} and the language has been discussed extensively by software developers.\footnote{\url{https://medium.com/@octskyward/why-kotlin-is-my-next-programming-language-c25c001e26e3}}

\subsection{Package Diagram}

Figure~\ref{fig:package} shows how \rspdf is divided into packages and provides a high-level overview of its internal architecture.

\begin{figure}[!tbp]
\centering
\includegraphics[scale=0.7]{figures/package.png}
\caption{Package diagram of \rspdf.}
\label{fig:package}
\end{figure}

Interaction with \rsserver begins in the \textbf{program} package. The \emph{Program} object parses and validates the command-line options before delegating to the publication controller.

The \textbf{utils} package provides shared filesystem defaults, program constants, and logging configuration. Server addresses are supplied through the required URL argument and are not hard-coded as constants.

The \textbf{publications} package contains the PDF business logic. Its Controller/Model separation follows the same object-oriented intent as MVC without depending on Rails: the controller discovers one or more PDF files and creates the corresponding models, while each model loads a document, adds the link and QR code, writes metadata, and saves a new file. A View is unnecessary because the result is the edited PDF itself.

\subsection{Class Diagram}

Figure~\ref{fig:classi-2} shows the main classes of \rspdf. The structure follows Controller/Model responsibilities: \emph{Program} owns process-level parsing and flow, \emph{PublicationController} manages the collection, and \emph{PublicationModel} performs PDF transformations through PDFBox and ZXing.

The \emph{Parameters} data class transports the caption, URL, optional authentication token, and optional publication identifier through the controller boundary. This keeps the \emph{PublicationModel} interface stable as related input data evolve.

\begin{figure}[!tbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[node distance=1.7cm and 1.8cm]
  \node[rsclass] (program) {
    \textbf{Program}
    \nodepart{second}
    main(arguments)\\
    loadCommandLineOptions()
  };
  \node[rsclass, right=of program] (controller) {
    \textbf{PublicationController}
    \nodepart{second}
    models: List\\
    load(input, output)\\
    addUrl(parameters)
  };
  \node[rsclass, right=of controller] (model) {
    \textbf{PublicationModel}
    \nodepart{second}
    loadData(file, output)\\
    addUrl(parameters)\\
    buildQRCode(url)
  };
  \node[rsclass, below=of controller] (parameters) {
    \textbf{Parameters}
    \nodepart{second}
    caption: String\\
    url: String\\
    authenticationToken: String?\\
    publicationIdentifier: String?
  };
  \node[rsclass, below=of program] (utils) {
    \textbf{Constants / Tools}
    \nodepart{second}
    filesystem defaults\\
    logging configuration
  };
  \node[rsbox, right=of model] (libraries) {
    \textbf{External libraries}\\[2pt]
    Apache PDFBox 3\\
    ZXing\\
    Log4j 2
  };

  \draw[rsarrow] (program) -- node[above] {\scriptsize creates} (controller);
  \draw[rsarrow] (controller) -- node[above] {\scriptsize manages $1..*$} (model);
  \draw[rsdependency] (program) -- (parameters);
  \draw[rsdependency] (controller) -- (parameters);
  \draw[rsdependency] (model) -- (parameters);
  \draw[rsdependency] (program) -- (utils);
  \draw[rsdependency] (controller) -- (utils);
  \draw[rsdependency] (model) -- (libraries);
\end{tikzpicture}%
}
\caption{High-level class diagram of \rspdf v2.0.0.}
\label{fig:classi-2}
\end{figure}

\subsection{Installation}

\rspdf is bundled with compatible \rsserver distributions, eliminating the need for a separate installation. To use it independently, download the shaded JAR from the repository releases\footnote{\url{https://github.com/Miccighel/Readersourcing-2.0-RS_PDF}} or build it from source:

\begin{lstlisting}
./mvnw clean verify
\end{lstlisting}

On Windows use \verb|mvnw.cmd clean verify|. The Maven Wrapper selects the configured Maven version and the executable is generated as \verb|target/RS_PDF-v2.0.0-shaded.jar|.

\subsubsection{Requirements}

\begin{itemize}
\item Java runtime $>=$ 21 to execute the shaded JAR;
\item JDK $>=$ 21 to compile and verify the project from source.
\end{itemize}

\subsubsection{Command Line Interface}

The behavior of \rspdf is configured through the command-line options in Table~\ref{tab:cli}. Caption and URL are always required. Input and output paths may be omitted only together, in which case the default \verb|data| and \verb|res| directories are used. The authentication token and publication identifier are optional, but if either is supplied then both custom paths and both server values must be supplied.

\small
\begin{longtable}{L{1.1cm}L{2.6cm}L{6.0cm}L{2.5cm}}
\toprule
\textbf{Short} & \textbf{Long} & \textbf{Description} & \textbf{Constraint} \\
\midrule
\endhead
\verb|-pIn| & \verb|--pathIn| & Input PDF file or directory, using a relative or absolute path. & Optional; requires \verb|-pOut|. \\
\midrule
\verb|-pOut| & \verb|--pathOut| & Existing output directory for annotated PDF files. & Optional; requires \verb|-pIn|. \\
\midrule
\verb|-c| & \verb|--caption| & Caption of the clickable link added to the PDF. & Required. \\
\midrule
\verb|-u| & \verb|--url| & Rating URL used by the link, QR code, and \verb|BaseUrl| metadata. & Required valid URL. \\
\midrule
\verb|-a| & \verb|--authToken| & Authentication token received from \rsserver. & Optional; requires paths and \verb|-pId|. \\
\midrule
\verb|-pId| & \verb|--publicationId| & Publication identifier received from \rsserver. & Optional; requires paths and \verb|-a|. \\
\bottomrule
\caption{Command line options of \rspdf.}
\label{tab:cli}
\end{longtable}
\normalsize

The following example assumes that:

\begin{itemize}
    \item there is a folder containing $n$ PDF files at \verb|C:\data|;
    \item annotated files must be saved in the existing folder \verb|C:\out|;
    \item the shaded JAR is located at \verb|C:\lib\RS_PDF-v2.0.0-shaded.jar|.
\end{itemize}

\rspdf can then be executed with the following command:

\begin{lstlisting}[basicstyle=\scriptsize\ttfamily]
java -jar C:\lib\RS_PDF-v2.0.0-shaded.jar \
  -pIn C:\data -pOut C:\out \
  -c "Express your rating" \
  -u "https://readersourcing.example/rate"
\end{lstlisting}

\section{\rsrate}

\label{rs-rate}

\rsrate~\cite{RS-Rate} is an extension designed to function as a client for the Readersourcing 2.0 ecosystem without requiring access to its website. Compatible with Google Chrome,\footnote{\url{https://www.google.com/chrome/}} Microsoft Edge,\footnote{\url{https://www.microsoft.com/en-us/edge/}} and Mozilla Firefox,\footnote{\url{https://www.mozilla.org/firefox/}} the extension allows users to rate publications directly from their browsers. This eliminates the need to navigate to the main website, streamlining the process of providing ratings for publications.

The primary objective of \rsrate is to enable readers to rate a publication with minimal effort: just a few clicks or keystrokes within the Readersourcing 2.0 ecosystem. In doing so, it contributes to a more dynamic online reading experience. \rsrate is the first client developed for the project beyond the web-based interface available on the main portal.

Looking ahead, our vision includes expanding the compatibility of \rsrate to Safari and other popular browsers. Our commitment is to make this rating tool accessible across a broad range of browsers, ensuring users can seamlessly interact with content and provide feedback regardless of their preferred browser. The current Chromium and Firefox targets share the same application source and rating logic.

\subsection{Implementation and Technology}

\rsrate is an extension for Google Chrome, Microsoft Edge, and Mozilla Firefox. These extensions are developed using standard web technologies such as HTML, CSS, and JavaScript. They are collections of files packaged according to the requirements of each browser. The build process copies and checks all executable assets locally, then produces unpacked Manifest V3 extensions in \verb|dist/chromium| and \verb|dist/firefox|.

The JavaScript component also uses jQuery to simplify DOM selection and manipulation, event management, animations, and AJAX operations. \rsrate relies on these AJAX features to provide responsive interactions with the server. Bootstrap 4 and its jQuery-based plugins provide the markup and interaction behavior used by the extension views. Dependencies remain declared in \verb|package.json| and are resolved through the committed Yarn lockfile.

\subsection{Installation}

\rsrate is freely available on the Google Chrome Web Store. To use it with a Chromium-based browser, follow the link and install the currently available version from the store page:
\begin{itemize}
\item \textbf{Google Chrome and Microsoft Edge}: \url{https://chromewebstore.google.com/detail/readersourcing-20-rsrate/hlkdlngpijhdkbdlhmgeemffaoacjagg}
\end{itemize}

The repository also produces a Firefox Manifest V3 package ready for local validation and submission to Mozilla Add-ons. It uses the following stable Gecko identifier:
\begin{center}
\small\path{rs-rate@readersourcing.org}
\end{center}
For local testing, open \path{about:debugging}, choose \emph{This Firefox}, and load
\begin{center}
\small\path{dist/firefox/manifest.json}
\end{center}
using \emph{Load Temporary Add-on}. Distribution through Mozilla Add-ons requires validation and signing by Mozilla.

\subsection{Development and Packaging}

The development toolchain uses Node.js 24 LTS and Yarn 4.17.1. From the repository root:

\begin{lstlisting}
corepack enable
yarn install --immutable
yarn verify
\end{lstlisting}

\verb|yarn verify| builds both targets, checks that all runtime assets are local, validates the Firefox build with Mozilla's \verb|web-ext|, and runs the client contract tests. The Firefox archive intended for Mozilla Add-ons is generated with:

\begin{lstlisting}
yarn package:firefox
\end{lstlisting}

The ZIP is written to \verb|artifacts|. The Firefox target requires Firefox 142 or later. Its manifest declares the account, browsing, website-interaction, and publication-content categories required by the Readersourcing workflow. The extension does not include an analytics channel.

Development builds initially target \url{http://localhost:3000}. Users can change the \rsserver host in the extension options, and the same configured host is used for normal API requests and PDF extraction. When the value is saved, the browser asks the reader to grant access to that HTTP or HTTPS origin. The permission covers only the selected origin, while any configured application path remains part of the server address.

\subsection{Usage}

Figure~\ref{fig:ux-1} shows a Google Chrome window with the extension active for a publication. This represents a typical reader visiting a publisher's website to access a paper in PDF format. The figure also shows the first page displayed when the reader opens the client. From this page, the reader can reach either the login page shown in Figure~\ref{fig:login} or the sign-up page. The login page also provides access to a password-recovery page, which requests the email containing the link to the \rsserver password form.

\begin{figure}
\centering
\includegraphics[width=\textwidth]{figures/ux-1.png}
\caption{\rsrate characterized as an extension having a popup action.}
\label{fig:ux-1}
\end{figure}

\begin{figure}
\centering
\includegraphics[scale=0.7]{figures/login.png}
\caption{The login page of \rsrate.}
\label{fig:login}
\end{figure}

If a reader has yet to sign up for Readersourcing 2.0, they can navigate from the page shown in Figure~\ref{fig:ux-1} to the one displayed in Figure~\ref{fig:signup} and fill in the sign-up form. Once they complete the standard sign-up and login operations, they will find themselves on the rating page, as shown in Figure~\ref{fig:rating-1}.

\begin{figure}
\centering
\includegraphics[scale=0.7]{figures/rating-2.PNG}
\caption{The rating page of \rsrate.}
\label{fig:rating-1}
\end{figure}

\begin{figure}
\centering
\includegraphics[scale=0.7]{figures/signup.PNG}
\caption{The user registration page of \rsrate.}
\label{fig:signup}
\end{figure}


In the central section of the rating page, a reader can use the slider to choose a value in the 0--100 interval. After selecting the desired value, they submit the rating by clicking the green \emph{Rate} button. The options page also allows the reader to make the rating anonymous. Readers must still be logged in when submitting an anonymous rating so that the system can prevent spam and repeated ratings of the same publication. Apart from these checks, the rating is processed without exposing the reader's identity.

If the reader prefers to rate the publication later, they can click the \emph{Save for later} button. This option starts the publication-editing procedure, which stores a reference URL inside the PDF file being viewed. Once the procedure is complete, the \emph{Save for later} button becomes a \emph{Download} button, as shown in Figure~\ref{fig:rating-2}.

\begin{figure}
\centering
\includegraphics[scale=0.7]{figures/rating.PNG}
\caption{The rating page of \rsrate after a \lq\lq save for later\rq\rq{} request.}
\label{fig:rating-2}
\end{figure}

The reader can then download the link-annotated publication. The refresh button, located to the right of the \emph{Download} button, retrieves a new copy of the publication, annotates it, and makes it available again. This feature is useful when an author updates a publication after the first download.

The downloaded PDF contains a new final page with the rating URL. The URL includes the encrypted paper-rating reference associated with the reader and publication, not the reader's JWT. The reference has no automatic expiration and remains independent of login and logout operations. Figure~\ref{fig:reference} shows an example opened in a PDF reader.

\begin{figure}
\centering
\includegraphics[width=\textwidth]{figures/reference.png}
\caption{A publication link-annotated through \rsrate.}
\label{fig:reference}
\end{figure}

Following the reference takes the reader to a dedicated page in \rsserver. This interface allows them to rate the publication independently of the browser extension used to store the reference. For example, a reader can transfer the link-annotated publication to a tablet and use its built-in browser to submit the rating. Figure~\ref{fig:browser} shows the interface displayed after the reader follows the stored reference. The reader must have a current authenticated session, so another person who receives a copy of the publication cannot use the reference on their behalf. If the reader has logged out, they sign in again and reopen the same reference; the PDF itself does not need to be annotated again.

\begin{figure}
\centering
\includegraphics[scale=0.7]{figures/browser.png}
\caption{The server-side interface to rate a publication.}
\label{fig:browser}
\end{figure}

Whenever a reader rates a publication, \rsserver updates the scores computed by both the RSM and TRM models. \rsrate displays the two publication scores at the bottom of the rating page, as shown in Figures~\ref{fig:rating-1} and~\ref{fig:rating-2}. The reader can view their own scores by clicking the profile button in the upper-right corner. The profile page shown in Figure~\ref{fig:profile} also allows the reader to change their password.

\begin{figure}
\centering
\includegraphics[scale=0.7]{figures/profile.png}
\caption{The profile page of \rsrate.}
\label{fig:profile}
\end{figure}

\section{\rspy}

\label{rs-py}

\rspy \cite{RS-Py} is an additional component of the Readersourcing 2.0 ecosystem, providing a fully functional Python implementation of the models presented by \citet{Soprano2019}. These models are also encapsulated by the server-side application of Readersourcing 2.0, as described in Section~\ref{rs-server}.

Developers familiar with Python can use \rspy to generate and test new simulations of readers rating a set of publications. They can alter the internal logic of the models to test new approaches without having to fork and edit the full Readersourcing 2.0 implementation.

\subsection{Implementation and Technology}

\rspy is a collection of \emph{Jupyter Notebooks}. Jupyter\footnote{\url{https://jupyter.org/}} is an open-source web application powered by Python\footnote{\url{https://www.python.org/}} for creating and sharing documents containing live code, equations, visualizations, and narrative text. Notebooks are composed of cells that can be run independently, providing a step-by-step overview of the implemented computations.

\subsection{Installation}

To use the \rspy notebooks, clone the repository\footnote{\url{https://github.com/Miccighel/Readersourcing-2.0-RS_Py}} and place it somewhere on the filesystem. The project targets Python 3.13, and its tested direct dependencies are declared in \verb|requirements.txt|. An isolated environment can be created as follows:

\begin{lstlisting}
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
\end{lstlisting}

On Windows, activate the environment with \verb|.venv\Scripts\activate|.

\subsection{Usage}

\rspy is organized into the following main folders:

\begin{itemize}
    \item The \verb|data| folder stores the datasets used to test the models presented by \citet{Soprano2019} and the compressed synthetic datasets.
    \item The \verb|models| folder stores the output of these models and the computed quantities.
    \item The \verb|notebooks| folder contains Jupyter notebooks used to generate datasets, implement the models, and analyze experiments.
    \item The \verb|scripts| folder is generated on demand and contains implementations of the Jupyter notebooks as pure Python scripts.
    \item The \verb|src| folder contains the conversion utility and the in-memory domain implementation.
    \item The \verb|tests| folder contains the numerical and utility characterization tests.
    \item The \verb|utils| folder contains auxiliary tooling, including the power-law generator.
\end{itemize}

Within the \verb|notebooks| folder, the principal files are:

\begin{itemize}
    \item \verb|1.Seed-Power-Law.ipynb| generates synthetic data using power-law distributions;
    \item \verb|2.Readersourcing.ipynb| implements and runs the RSM model;
    \item \verb|3.TrueReview.ipynb| implements and runs the TRM model;
    \item \verb|4.Experiments.ipynb| supports experimental analysis;
    \item \verb|ReadersourcingToolkit.py| contains shared Python functionality used by the notebooks.
\end{itemize}

The \verb|src/Convert.py| script converts the numbered notebooks into pure Python scripts and stores them in the generated \verb|scripts| folder together with their shared toolkit.

The behavior of the notebooks can be customized by modifying the parameter settings found in their initial cells. To run them, navigate to the \verb|notebooks| folder and type \verb|jupyter lab|. This command starts the Jupyter server and opens the interface from which the notebooks can be executed in numerical order.

The in-memory implementation under \verb|src/rs_py| and its characterization tests reproduce the reader, publication, and rating behavior currently shared with \rsserver. They complement rather than restrict the principal purpose of \rspy: developers remain free to alter the internal logic of the notebooks and test new approaches. Additional object-oriented simulation work, including an explicit author entity, is maintained in the Readersourcing\_OO repository.\footnote{\url{https://github.com/EddyMaddalena/Readersourcing_OO}}

\section{Overview}

\label{overview}

Figure~\ref{fig:scenario} summarizes the capabilities of Readersourcing 2.0. In this example, four readers---\verb|RD1|, \verb|RD2|, \verb|RD3|, and \verb|RD4|---use \rsrate to rate publication \verb|P1|. Readers \verb|RD1|, \verb|RD2|, and \verb|RD3| choose \emph{Save for later} and receive a link-annotated version of the publication, denoted by \verb|P1+Link|.

After some time, \verb|RD1| opens \verb|P1+Link| with a preferred PDF reader. \verb|RD2| sends it to a tablet, while \verb|RD3| opens it in a desktop browser. By following the link or scanning the QR code added through \rspdf, they reach the dedicated \rsserver page and provide their ratings. In contrast, \verb|RD4| rates \verb|P1| immediately through the \rsrate interface.

\begin{figure}
\centering
\includegraphics[width=\textwidth]{figures/scenario.png}
\caption{Readers' interaction modalities with the Readersourcing 2.0 ecosystem.}\label{fig:scenario}
\end{figure}

\newpage

\nocite{*}
\printbibliography[heading=bibintoc]

\end{document}
